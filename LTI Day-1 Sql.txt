DDL STATEMENT
-----------------
1)To create table in sql

create table Person
(
Pid number PRIMARY KEY,
FirstName varchar2(20),
lastname varchar2(20),
salary number
);
create table ProductInfo
(
Prodid number PRIMARY KEY,
ProdName varchar2(20),
Category varchar2(20),
Price number
);

create table Student
(
sid number PRIMARY KEY,
sname varchar2(20),
dob varchar2(20),
collegename varchar2(20)
);
create table Course
(
cid number PRIMARY KEY,
cname varchar2(20),
fee number,
duration number
);
create table enroll
(
sid number FOREIGN KEY,cid number FOREIGN KEY,
doj varchar2(20)
);


create sequence prodSeq
start with 100
increment by 1
nocycle
nocache;
truncate table ProductInfo;
Select * from ProductInfo;
insert into ProductInfo values(prodSeq.NEXTVAL,'Colgate',45,'Paste');

Desc Person

2)Modifying Table Structure

Adding and Drop Column

Alter Table Person ADD age number;
Alter Table Person DROP column salary;  

To Drop(delete) Table
Drop table person; 

Format table data
 truncate table person;
Difference between truncate and drop - in truncate data will be deleted but table will remain.


Data Manipulation language
--------------------------

1. Insert into person values(101,'Mark','walker',10000);
   Insert into person(PID,firstName,LastName,Salary) values(102,'arya','snow',10000);
    Insert into person(PID,firstName) values(101,'Mark');   //Partial Insert
  

2. update person set lastname='eagen' where pid = '102';
   update person set salary=salary +2000;

3. Delete from Person where pid = 102;
   Delete from Person where salary>8000;
To make copy of table 
Merge:  Create table PersonBackup as select* from Person;  //to clone table
 
Merge into PersonBackup p2 
	using person p1
on (p2.pid = p1.pid)
when matched then 
	update set p2.firstname = p1.FirstName,P2.Lastname = p1.Lastname,p2.salary = p1.salary
when not matched then
	Insert into person(PID,firstName,LastName,Salary) values(p1.pid,p1.Firstname,p1.lastname,p1.salary)

update person set lastname = 'Roshani' where pid=101;


DCL
---------------------------
grant,revoke
create user dummy identified by password;
grant create session to dummy;
revoke create session to dummy;


Primary key--
create table Person
(
Pid number PRIMARY KEY,
FirstName varchar2(20),
);

create table Person2
(
Pid number, 
constraint name primarykey(pid)
FirstName varchar2(20),
);

check constraint
create table Person2
(
Age number CHECK, 
city Varchar2(20) CHECK (CIty IN('Chennai','Mumbai'))
Salary number CHECK(salary,
);


create or replace procedure insertProd(Pid number,name varchar,price number,cat varchar)
as
begin
	insert into productInfo values(pid,name,price,cat)
	commit;
end;
/
create or replace procedure insertProd(ProdID number,ProdName varchar, Category varchar,Price number)
as
begin
	insert into productInfo values(ProdID,ProdName,Category,Price);
	commit;
end;
/